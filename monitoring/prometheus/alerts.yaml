groups:
- name: bms_alerts
  interval: 10s
  rules:

  - alert: ServiceDown
    expr: up{job="bms-backend"} == 0
    for: 15s
    labels:
      severity: critical
    annotations:
      summary: "🔥 Backend упал и не встаёт!"
      description: "😱 Сервис bms-backend недоступен уже 15 секунд!\n\nPrometheus не может собрать метрики.\nВероятно, что кто-то чутка перестарался... \n\nРасчехляй бэкапчик братанчик)"
      resolved_description: "🎉 Backend вернулся в строй! Система снова в деле!\n\nМетрики текут, сервис отвечает. Можно выдохнуть и не открывать бэкап) Чайку попей 🫖"

  - alert: DatabaseDown
    expr: pg_up == 0
    for: 15s
    labels:
      severity: critical
    annotations:
      summary: "🚨 Критический сбой: база данных упала!"
      description: "😱 Сердце системы остановилось. Индикатор жизнеспособности PostgreSQL стабильно показывает ноль в течение 15 секунд.\n"
      resolved_description: "🎉 Сердце вновь забилось! Пристигнитесь, мы взлетаем! База данных восстановлена."

  - alert: PostgresExporterDown
    expr: up{job="bms-db"} == 0
    for: 15s
    labels:
      severity: critical
    annotations:
      summary: "🔥 bms-postgres-exporter упал и не встаёт!"
      description: "😱 Система мониторинга ослепла. Наш брат bms-postgres-exporter перестал отвечать на запросы Prometheus."
      resolved_description: "Наш брат снова прозрел 👁 \n\nbms-postgres-exporter восстановлен и снова следит за БД как ястреб 🦅"

  - alert: High5xxErrorRate
    expr: |
      (
        sum(rate(http_requests_total{job="bms-backend",status=~"5.."}[5m]))
        /
        sum(rate(http_requests_total{job="bms-backend"}[5m]))
      ) > 0.05
      and
      sum(rate(http_requests_total{job="bms-backend"}[5m])) > 1
    for: 30s
    labels:
      severity: critical
    annotations:
      summary: "💥Высокий уровень ошибок 5XX в сервисе \nСервер плюётся ошибками 5XX!"
      description: "🚨 Более 5% запросов возвращают 5XX ошибки уже 30 секунд!\n\nЭто указывает на проблемы на стороне сервера. Проверьте логи приложения, состояние базы данных и системные ресурсы."
      resolved_description: "🥳 Ура! Сервер перестал плеваться, всё работает как часы ⚙️"

  - alert: High4xxErrorRate
    expr: |
      (
        sum(rate(http_requests_total{job="bms-backend",status=~"4.."}[5m]))
        /
        sum(rate(http_requests_total{job="bms-backend"}[5m]))
      ) > 0.10
      and
      sum(rate(http_requests_total{job="bms-backend"}[5m])) > 1
    for: 1m
    labels:
      severity: warning
    annotations:
      summary: "⚠️Повышенный уровень ошибок 4XX в сервисе! \nКлиенты не могут попасть куда нужно"
      description: "🤔 Более 10% запросов возвращают 4XX ошибки уже минуту.\n\nВозможные причины: некорректные запросы клиентов, устаревшие API-эндпоинты или проблемы аутентификации/авторизации."
      resolved_description: "👍 Клиенты снова находят дорогу! Уровень 4XX ошибок нормализовался.\n\nПути API снова ясны и понятны для всех 🗺"

  - alert: SlowAPIResponse
    expr: |
      histogram_quantile(
        0.95,
        sum(rate(http_request_duration_seconds_bucket{job="bms-backend"}[5m]))
        by (le)
      ) > 1
    for: 1m
    labels:
      severity: warning
    annotations:
      summary: "Высокая задержка обработки запросов! \n🐌 API работает как улитка"
      description: "⏱️ 95-й перцентиль времени ответа сервиса bms-backend превышает 1 секунду в течение минуты. Рекомендуется проверить производительность базы данных, оптимизировать запросы и проверить системные ресурсы."
      resolved_description: "⚡️ Улитку заменил гепард! Задержки вернулись в норму!\n\nAPI снова быстрый и отзывчивый, пользователи довольны 😎"

  # - alert: TrafficDrop
  #   expr: sum(rate(http_requests_total{job="bms-backend"}[5m])) < 1
  #   for: 2m
  #   labels:
  #     severity: warning
  #   annotations:
  #     summary: "📉 Аномальное снижение трафика сервиса bms-backend"
  #     description: "😴 Средняя частота запросов к сервису bms-backend упала ниже 1 запроса в секунду в течение 2 минут. Рекомендуется проверить доступность сервиса для пользователей, сетевую маршрутизацию и внешние зависимости."
  #     resolved_description: "📈 Трафик вернулся! Пользователи снова активны!\n\nСервис снова востребован, запросы текут рекой 🌊"

  # - alert: BackendPanic
  #   expr: increase(panic_total{job="bms-backend"}[5m]) > 0
  #   for: 0m
  #   labels:
  #     severity: critical
  #   annotations:
  #     summary: "💀 Backend паникует!"
  #     description: "🚨 Обнаружен panic в backend!\n\nКод решил что жизнь слишком сложна и упал в обморок 💀\n\nЭто критично! Скорее всего:\n- nil pointer dereference (классика) 🎯\n- Кто-то забыл обработать ошибку 😱\n- Go решил что пора на пенсию 🏖️\n- Деление на ноль (математика сложная) ➗\n\nСРОЧНО смотри логи и фикси! 🏃💨\nИ в следующий раз пиши тесты! 🧪"

- name: business_metrics_alerts
  interval: 10s
  rules:

  - alert: HighBookingCancellationRate
    expr: |
      (
        sum(rate(booking_cancelled_total[5m]))
        /
        sum(rate(booking_created_total[5m]))
      ) > 0.5
      and
      sum(rate(booking_created_total[5m])) > 0.1
    for: 5m
    labels:
      severity: warning
    annotations:
      summary: "⚠️ Высокий процент отмен бронирований"
      description: "🚨 Более 50% бронирований отменяется в течение последних 5 минут.\n\nЭто может указывать на проблемы с:\n- Удобством процесса бронирования\n- Качеством информации о ресурсах\n- Техническими проблемами системы\n\nРекомендуется проверить логи и провести анализ причин отмен."
      resolved_description: "✅ Процент отмен бронирований вернулся в норму.\n\nПользователи снова довольны процессом бронирования! 🎉"

  - alert: NoBookingsCreated
    expr: sum(rate(booking_created_total[10m])) == 0
    for: 15m
    labels:
      severity: warning
    annotations:
      summary: "📉 Нет новых бронирований в течение 15 минут"
      description: "😴 Система не получает новых бронирований уже 15 минут.\n\nВозможные причины:\n- Проблемы с доступностью сервиса\n- Снижение активности пользователей\n- Технические проблемы с процессом бронирования\n\nРекомендуется проверить доступность API и бота."
      resolved_description: "📈 Бронирования возобновились! Система снова работает! 🎉"

  - alert: HighBotMessageProcessingTime
    expr: |
      histogram_quantile(
        0.95,
        sum(rate(bot_message_processing_seconds_bucket[5m])) by (le, bot_id)
      ) > 5
    for: 2m
    labels:
      severity: warning
    annotations:
      summary: "🐌 Высокое время обработки сообщений бота"
      description: "⏱️ 95-й перцентиль времени обработки сообщений бота превышает 5 секунд в течение 2 минут.\n\nЭто может указывать на:\n- Проблемы с производительностью бота\n- Медленные запросы к базе данных\n- Проблемы с внешними API\n\nРекомендуется проверить производительность системы и оптимизировать обработчики."
      resolved_description: "⚡️ Время обработки сообщений вернулось в норму!\n\nБот снова быстрый и отзывчивый! 😎"

  - alert: BotMessageRateDrop
    expr: sum(rate(bot_messages_total[5m])) < 0.01
    for: 10m
    labels:
      severity: warning
    annotations:
      summary: "📉 Резкое снижение активности бота"
      description: "😴 Количество сообщений бота упало ниже 0.01 сообщений в секунду в течение 10 минут.\n\nВозможные причины:\n- Бот недоступен для пользователей\n- Проблемы с Telegram API\n- Снижение активности пользователей\n\nРекомендуется проверить статус бота и доступность Telegram API."
      resolved_description: "📈 Активность бота восстановилась!\n\nПользователи снова активно используют бота! 🎉"