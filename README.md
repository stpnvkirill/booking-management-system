# Booking Management System (BMS)

Cервис управления бронированием ресурсов организации (переговорные комнаты, оборудование и т.д.) с интеграцией Telegram-бота, системой уведомлений и поддержкой обратной связи.

## 📋 Описание

RESTful API для управления бронированием ресурсов и интегрированного Telegram-бота для удобного взаимодействия пользователей. Система поддерживает:

- ✅ **Управление пользователями** - регистрация, аутентификация
- ✅ **Разделение по заказчикам** - отдельные пространства
- ✅ **Telegram-интеграция** - управление несколькими ботами, обработка команд и сообщений
- ✅ **Логирование** - подробное логирование всех операций
- ✅ **Database migrations** - управление схемой БД через Alembic

## 🛠️ Технологический стек

### Backend
- **FastAPI** - современный веб-фреймворк с асинхронной поддержкой
- **SQLAlchemy** - ORM для работы с БД
- **Alembic** - управление миграциями БД
- **asyncpg** - асинхронный драйвер PostgreSQL
- **aiogram** - фреймворк для Telegram-ботов

### Infrastructure
- **PostgreSQL** - основная база данных
- **Redis** - кеширование и FSM хранилище для ботов
- **Docker & Docker Compose** - контейнеризация и оркестрация

### Developer Tools
- **Ruff** (v0.14.8+) - быстрый линтер и форматер Python
- **Pre-commit** (v4.5.0+) - git хуки для проверки кода

## 📁 Структура проекта

```
BMS/
├── app/                          # Основной пакет приложения
│   ├── api/                          # REST API endpoints
│   ├── bot/                          # Telegram-бот логика
│   ├── domain/                       # Бизнес-логика
│   ├── infrastructure/               # Инфраструктура
│   ├── config/                       # Конфигурация приложения
│   ├── middlewares/                  # Глобальные middleware
│   ├── app.py                        # Инициализация FastAPI приложения
│   └── depends.py                    # Зависимости
├── main.py                       # Точка входа приложения
├── pyproject.toml                # Конфигурация проекта и зависимости
├── Dockerfile                    # Docker образ для контейнера
├── docker-compose.yaml           # Конфигурация docker-compose
├── alembic.ini                   # Конфиг Alembic для миграций
└── README.md                     # Этот файл
```

## 🚀 Быстрый старт

### Требования
- **Python**
- **UV**
- **Docker** и **Docker Compose** (для развертывания)
- **.env** файл с конфигурацией (см. примеры ниже)

### Установка зависимостей

```bash
uv sync
pre-commit install
```

### Локальное развертывание

#### 1. Создать .env файл

```bash
EXCEPT_LOG=true

POSTGRES_USER=bms_user
POSTGRES_PASSWORD=bms_pwd
POSTGRES_DB=bms_db
POSTGRES_HOST=localhost
POSTGRES_PORT=6432

USE_REDIS_STORAGE=true

ADMINBOT_TOKEN = "<Ваш токен для админ бота (взять в botfather)>"
ADMINBOT_ID = "<Ваш Айди админ бота(взять в User InfoBot>"
```

#### 2. Запустить PostgreSQL и Redis (Docker)

```bash
docker-compose up -d
```

#### 3. Выполнить миграции БД

```bash
uv run alembic upgrade head
```

#### 4. Запустить приложение

```bash
# Локальный запуск с автоперезагрузкой
uv run main.py
```

API будет доступно по адресу: `http://localhost:8000`

Swagger документация: `http://localhost:8000/docs`

Токен для тестового пользователя: AZurPmBtfk6yU3lZ_Omf9A


#### 5. Запустить в докере

```bash
# Проверка работы в контейнере
docker compose --profile prod up
```

## 🤖 Telegram Bot

Система поддерживает управление несколькими Telegram-ботами.

### Регистрация бота

1. Создать бота через BotFather: `@BotFather`
2. Получить токен
3. Зарегистрировать в системе через API: `POST /api/telegram/bots`
4. Либо добавить переменные окружения для создания тестового пользователя:
```bash
CREATE_TEST_USER=true
TEST_USER_TLG_ID="<TLG_ID>"
TEST_BOT_TOKEN="<TOKEN>"
```

## 🗄️ База данных

### Миграции

Проект использует Alembic для управления схемой БД.

```bash
# Создать новую миграцию
uv run alembic revision --autogenerate -m "Описание изменений"

# Применить все миграции
uv run alembic upgrade head

# Отменить последнюю миграцию
uv run alembic downgrade -1
```


## 📝 Разработка

### Code Style

Проект использует **Ruff** для линтинга и форматирования. Настроено на уровне VSCode.

### Pre-commit Hooks

```bash
# Установить git хуки
pre-commit install

# Выполнить проверки вручную
pre-commit run --all-files
```

## 💡Админ бот

### Инструкция по проверке работы админ‑бота

1) Откройте файл .env
2) Добавить переменные окружения: ADMINBOT_TOKEN, ADMINBOT_ID
3) Запустить DBeaver
4) Открыть telegram, отправьте боту тестовое сообщение (@practika123_admin_bot)
5) Если бот отвечает — он работает корректно, и в таблицу bot_users добавятся данные

## 📊 Мониторинг

### Как запустить?
1. Устанавливаем loki-docker-driver
   ``` bash
   docker plugin install grafana/loki-docker-driver:latest --alias loki --grant-all-permissions
   ```
2. Устанавливаем и запускаем bms-db, bms-redis, bms-backend
   ``` bash 
   docker compose --profile prod up -d
   ```
3. Устанавливаем и запускаем bms-loki, bms-prometheus, bms-grafana
   ``` bash
   docker compose --profile monitoring up -d
   ```
   
### Доступ к интерфейсам 

После запуска будут доступны:
Grafana - http://localhost:3000
Prometheus - http://localhost:9090

Логин и пароль: admin, admin

### Проверка работоспобности

1. Заходим в Prometheus
2. Переходим в Status -> Target health
3. Вы должны увидеть bms-backend и prometheus в state = UP
4. Если у вас bms-backend в state = DOWN, то необходимо оставноить все профили, запустить профиль prod и ~10 секунд запусить проофиль monitoring

### Просмотр dashboard

1. Заходим в Grafana
2. После успешной авторизации вам предложут поменять пароль, в нашем случае это действие необходимо скипнуть
3. В меню слева нажимаем на Dashboards
4. Вы увидите папку BMS в которую нужно провалиться
5. После чего вы увидите dashboard под названием Backend, тыкаем по нему и дашборд покажется

### Как остановть?
1. Останавливаем bms-db, bms-redis, bms-backend
   ``` bash
   docker compose --profile prod stop
   ```
2. Останавливаем bms-loki, bms-prometheus, bms-grafana
   ``` bash 
   docker compose --profile monitoring stop
   ```

### Конфигурация компонентов 
```
BMS/
├── docker-compose.yaml           # Основная конфигурация docker-compose
├── docker-compose-prod.yaml      # Основная конфигурация docker-compose, которая будет использоваться на хосте
monitoring/
├── docker-compose.monitoring.yaml   # Основная конфигурация docker-compose, которая будет использоваться на хосте
├── grafana/
│   └── provisioning/
│       └── datasources/
│           ├── backend.yaml
│           ├── loki.yaml
│           └── prometheus.yaml
├── loki/
│   └── loki-config.yaml          # конфигурация Loki
└── prometheus/
    ├── prometheus.yaml           # конфигурация Prometheus
    └── alerts.yaml               # правила алертов
```
